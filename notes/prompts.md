1. one of the goals of this project is to allow an engineer to scaffold all of this infrastructure from scratch on a new set of servers with minimal effort and maximum ease thanks to a great user experience and maximum automation. what would the steps that this engineer would have to take look like? 2. i mentioned that we want to deploy apps like plex, radarr, sonarr, and bazarr. what would the process of deploying these applications look like for an engineer? would it be the same as the process to deploy an arbitrary dockerized HTTP service? 3. i mentioned we currently have two servers. will our deployment scheduler actually be able to deploy some applications to one server and other applications to the other server?
