---
# Deploy Traefik reverse proxy to workload VMs.
#
# Usage (invoked by `iac deploy proxy`):
#   ansible-playbook -i .generated/ansible/inventory/hosts.yml \
#     iac/ansible/playbooks/deploy-proxy.yml

- name: Deploy reverse proxy
  hosts: vms
  become: true
  gather_facts: true

  roles:
    - role: proxy-container
      tags: [proxy]
